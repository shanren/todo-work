# 轻量桌面 Todo 小窗 MVP 实现计划（M1–M3）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建开机自启、右上角小窗、三主题的本地待办工具（Windows/macOS，Tauri 2）。

**Architecture:** Rust 侧持有全部状态（`Mutex<State>`）并负责持久化与今日判定；前端为无框架 TypeScript 全量渲染视图；收起时销毁 WebView 窗口只留 Rust 进程以达成内存预算。

**Tech Stack:** Tauri 2 · 原生 TypeScript + Vite · JSON 持久化 · Vitest + cargo test

**Spec:** `docs/superpowers/specs/2026-09-17-todo-widget-design.md`

## Global Constraints

- **环境（Windows/Git Bash）**：所有 cargo/npm 命令前必须执行：
  `export PATH="/c/Users/hkzln/.cargo/bin:/c/Program Files/Microsoft Visual Studio/2022/Professional/VC/Tools/MSVC/14.44.35207/bin/Hostx64/x64:$PATH"`
  （否则 GNU link.exe 抢占 MSVC 链接器；Windows SDK 10.0.26100 已安装）

- 空闲内存：收起态 < 20MB；展开态 Windows < 80MB / macOS < 60MB（M1 实测，见 Task 1）
- 前端无框架、无 web 字体；DOM 节点 < 200
- 数据文件：`store.json`，原子写（临时文件 → rename），滚动备份 5 份
- 窗口 340px 宽（可 300/380），不可全屏，`decorations:false`
- 所有文案简体中文；ID 由前端 `crypto.randomUUID()` 生成
- 每个 Task 结束必须 commit；Rust 测试 `cargo test`、前端测试 `npx vitest run` 必须绿

---

### Task 0: 安装 Rust 工具链

**Files:** 无代码文件（环境准备）

- [x] **Step 1: 安装 rustup（Windows）**

```bash
winget install --id Rustlang.Rustup -e --accept-source-agreements --accept-package-agreements
# 或下载 https://win.rustup.rs/x86_64 的 rustup-init.exe 后执行 rustup-init -y --default-toolchain stable-msvc
```

macOS：`curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y`

- [x] **Step 2: 确认 MSVC 链接器可用（仅 Windows）**

运行 `cargo --version`。若报 link.exe 缺失：`winget install Microsoft.VisualStudio.2022.BuildTools`（勾选 "Desktop development with C++"），重开终端再验证。

- [x] **Step 3: 验证**

```bash
cargo --version && rustc --version
```

Expected: 输出版本号（stable ≥ 1.77，Tauri 2 的最低要求）。

- [x] **Step 4: Commit**

```bash
git add -A && git commit -m "chore: 记录 Rust 工具链安装完成" --allow-empty
```

### Task 1: Tauri 工程骨架 + 内存基线

**Files:**

- Create: 由 `create-tauri-app` 生成整个工程（vanilla-ts 模板）
- Create: `docs/memory-baseline.md`

- [x] **Step 1: 初始化工程与 git**

```bash
cd E:/workspace/todo-work
npm create tauri-app@latest todo-app -- --template vanilla-ts --manager npm --yes
cd todo-app && git init && npm install
```

- [x] **Step 2: 安装插件依赖**

```bash
npm install @tauri-apps/plugin-autostart @tauri-apps/plugin-single-instance
cd src-tauri && cargo add tauri-plugin-autostart tauri-plugin-single-instance && cd ..
```

（single-instance 仅 Rust 侧使用，npm 包可跳过；若装了不引用也无妨。）

- [x] **Step 3: 验证 dev 启动**

```bash
npm run tauri dev
```

Expected: 窗口弹出，显示模板页。首次 Rust 编译需数分钟，属正常。

- [x] **Step 4: 测量内存基线**

任务管理器（Windows）/ Activity Monitor（macOS）记录：Rust 进程 + WebView 进程合计空闲内存。写入 `docs/memory-baseline.md`（表格：日期 / 状态 / 数值 / 备注）。**门禁：展开态 Windows > 100MB 或 macOS > 80MB 时先排查（开发模式内存偏高属正常，以 release 构建复核）。**

- [x] **Step 5: Commit**

```bash
git add -A && git commit -m "chore: Tauri 工程骨架与内存基线记录"
```

### Task 2: Rust 数据模型与存储层（store.rs）

**Files:**

- Create: `src-tauri/src/store.rs`
- Modify: `src-tauri/src/main.rs`（声明 `mod store;`）

**Interfaces (Produces):**

- `struct Todo { id, title, category_id: Option<String>, color: Option<String>, due_date: Option<String>, due_time: Option<String>, done: bool, done_at: Option<String>, created_at: String, order: i64 }`
- `struct Category { id, name, color, order: i64 }`
- `struct Settings { theme, width, pos_x: Option<f64>, pos_y: Option<f64>, auto_start: bool, sort_mode, show_on_boot_only_today: bool }`
- `struct State { version: i32, categories: Vec<Category>, todos: Vec<Todo>, settings: Settings }`
- `State::load(dir: &Path) -> Result<(State, Option<String>)>`（返回恢复提示语）
- `State::save(&self, dir: &Path) -> Result<()>`（原子写 + 每日滚动备份）
- 全部 `#[derive(Serialize, Deserialize, Clone, Debug, PartialEq)]`，serde `rename_all = "camelCase"`

- [x] **Step 1: 写失败测试**（`src-tauri/src/store.rs` 底部 `#[cfg(test)]`）

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::fs;

    fn tmpdir() -> std::path::PathBuf {
        let d = std::env::temp_dir().join(format!("todo-test-{}", std::process::id()));
        fs::create_dir_all(&d).unwrap(); d
    }

    #[test]
    fn save_then_load_roundtrip() {
        let dir = tmpdir();
        let mut s = State::default();
        s.todos.push(Todo { id: "t1".into(), title: "测试".into(),
            category_id: None, color: None, due_date: Some("2026-09-17".into()),
            due_time: None, done: false, done_at: None,
            created_at: "2026-09-17T00:00:00Z".into(), order: 0 });
        s.save(&dir).unwrap();
        let (loaded, warn) = State::load(&dir).unwrap();
        assert!(warn.is_none());
        assert_eq!(loaded.todos[0].title, "测试");
        assert_eq!(loaded.todos[0].due_date.as_deref(), Some("2026-09-17"));
    }

    #[test]
    fn corrupt_json_recovers_from_backup() {
        let dir = tmpdir();
        let mut s = State::default(); s.save(&dir).unwrap();
        // 备份链滚动后 store.json 为损坏内容，.1 为良好副本
        let good = fs::read(dir.join("store.json")).unwrap();
        fs::write(dir.join("store.json.1"), &good).unwrap();
        fs::write(dir.join("store.json"), b"{broken").unwrap();
        let (loaded, warn) = State::load(&dir).unwrap();
        assert!(warn.is_some(), "应返回恢复提示");
        assert_eq!(loaded, s);
    }

    #[test]
    fn rolling_backup_keeps_five() {
        let dir = tmpdir();
        for i in 0..9 {
            let mut s = State::default();
            s.version = i; // 内容变化触发新备份
            s.save(&dir).unwrap();
        }
        let mut n = 0;
        for i in 1..=6 { if dir.join(format!("store.json.{i}")).exists() { n += 1 } }
        assert_eq!(n, 5, "只保留 5 份滚动备份");
    }
}
```

- [x] **Step 2: 运行确认失败**

Run: `cd src-tauri && cargo test`
Expected: 编译失败（`State` 未定义）。

- [x] **Step 3: 实现 store.rs**

要点：`serde` 序列化；`save()` 先写 `store.json.tmp` 再 `fs::rename`；备份策略——若 `store.json` 存在且距上次备份跨自然日，将 `.4→.5 .3→.4 … .1→.2 store.json→.1` 逐个 rename；`load()` 依次尝试 `store.json`、`.1`…`.5`，首个解析成功者回写主文件并返回 `Some("数据已从备份恢复")`；全损坏则返回默认 `State` + 警告语。`State::default()` 提供 `version:1`、默认 settings（`theme:"glass", width:340, sort_mode:"manual", …`）。

- [x] **Step 4: 运行测试通过**

Run: `cd src-tauri && cargo test`
Expected: 3 个测试 PASS。

- [x] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(rust): 数据模型与原子存储层"
```

### Task 3: 今日/过期判定（today.rs）

**Files:**

- Create: `src-tauri/src/today.rs`
- Modify: `src-tauri/src/main.rs`（`mod today;`）

**Interfaces (Produces):**

- `enum Bucket { Overdue, Today, Later, Done }`
- `fn bucket(todo: &Todo, today: NaiveDate) -> Bucket`（`chrono::NaiveDate`）
- `fn has_today(state: &State, today: NaiveDate) -> bool`

- [x] **Step 1: 写失败测试**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::store::Todo;
    fn t(due: Option<&str>, done: bool) -> Todo {
        Todo { id: "x".into(), title: "x".into(), category_id: None, color: None,
            due_date: due.map(String::from), due_time: None, done, done_at: None,
            created_at: String::new(), order: 0 }
    }
    let today = chrono::NaiveDate::from_ymd_opt(2026, 9, 17).unwrap();

    #[test] fn due_today_is_today()      { assert!(matches!(bucket(&t(Some("2026-09-17"), false), today), Bucket::Today)); }
    #[test] fn due_future_is_later()     { assert!(matches!(bucket(&t(Some("2026-09-18"), false), today), Bucket::Later)); }
    #[test] fn due_past_is_overdue()     { assert!(matches!(bucket(&t(Some("2026-09-16"), false), today), Bucket::Overdue)); }
    #[test] fn no_due_undone_is_today()  { assert!(matches!(bucket(&t(None, false), today), Bucket::Today)); }
    #[test] fn done_is_done()            { assert!(matches!(bucket(&t(Some("2026-09-17"), true), today), Bucket::Done)); }
    #[test] fn has_today_detects() {
        let mut s = crate::store::State::default();
        s.todos.push(t(Some("2026-09-17"), false));
        assert!(has_today(&s, today));
        s.todos[0].due_date = Some("2026-09-18".into());
        assert!(!has_today(&s, today));
    }
}
```

- [x] **Step 2: 运行确认失败** — `cargo test`，编译错误（模块不存在）。

- [x] **Step 3: 实现**（纯函数，规则见 Spec §4）+ `cargo add chrono` 于 `src-tauri`。

- [x] **Step 4: `cargo test` 全绿。**

- [x] **Step 5: Commit** — `git commit -m "feat(rust): 今日/过期分组判定"`

### Task 4: IPC 命令层（commands.rs）

**Files:**

- Create: `src-tauri/src/commands.rs`
- Modify: `src-tauri/src/main.rs`（注册命令、`tauri::Builder` 挂 `Mutex<State>`）

**Interfaces (Consumes):** Task 2 的 `State`、Task 3 的 `bucket`
**Interfaces (Produces):** Spec §8 全部命令；`add_todo` 内部实现排序键生成（`order = todos.len()`）并**立即 `state.save()`**（同步持久化，前端不再 debounce 写盘）；`delete_category` 将所属 todo `category_id` 置 `None`；`reorder(ids)` 按 ids 顺序重排未完成项 `order` 字段。

- [x] **Step 1: 实现**（命令为薄封装：`state.lock()` → 变更 → `save()` → 返回受影响实体；`Result<T, String>` 错误直通前端）。

- [x] **Step 2: 手动验证** — `npm run tauri dev`，在临时页用 DevTools console：

```js
await window.__TAURI__.core.invoke('add_todo', { title: '测试项', categoryId: null, dueDate: null, dueTime: null });
(await window.__TAURI__.core.invoke('get_state')).todos.length === 1
```

Expected: true，且数据目录出现 `store.json`。

- [x] **Step 3: `cargo test` 仍全绿。**

- [x] **Step 4: Commit** — `git commit -m "feat(rust): IPC 命令层"`

### Task 5: 前端状态模块（state.ts，Vitest）

**Files:**

- Create: `src/types.ts`（与 Spec §3 JSON 结构逐字段对齐的 TS 接口）
- Create: `src/state.ts`
- Create: `src/state.test.ts`

**Interfaces (Produces):**

- `class Store { state: AppState; sort(mode): void; visibleTodos(activeCat): Todo[]; groups(todos): {overdue, today, done} }`
- `buckets(todo, todayISO): 'overdue'|'today'|'later'|'done'`（与 Rust 规则一致的纯函数，用于渲染分组）
- 所有变更方法：乐观更新本地 state → `invoke(...)` → 失败回滚 + toast 回调

- [x] **Step 1: 写失败测试**（核心用例）

```ts
import { describe, it, expect } from 'vitest';
import { buckets, sortTodos } from './state';

const TODAY = '2026-09-17';
const todo = (o: Partial<any>) => ({ id: 'x', title: 'x', categoryId: null, color: null,
  dueDate: null, dueTime: null, done: false, doneAt: null, createdAt: '', order: 0, ...o });

describe('buckets', () => {
  it('今天到期 → today', () => expect(buckets(todo({ dueDate: TODAY }), TODAY)).toBe('today'));
  it('无到期未完成 → today', () => expect(buckets(todo({}), TODAY)).toBe('today'));
  it('昨天未完成 → overdue', () => expect(buckets(todo({ dueDate: '2026-09-16' }), TODAY)).toBe('overdue'));
  it('已完成 → done', () => expect(buckets(todo({ done: true }), TODAY)).toBe('done'));
});

describe('sortTodos', () => {
  const list = [ todo({ id: 'a', order: 2 }), todo({ id: 'b', order: 1 }),
    todo({ id: 'c', dueDate: '2026-09-16' }), todo({ id: 'd', done: true }) ];
  it('manual 按 order', () => expect(sortTodos(list, 'manual').map(t => t.id)).toEqual(['b','a','c','d']));
  it('due 按到期日', () => expect(sortTodos(list, 'due').map(t => t.id)).toEqual(['c','b','a','d']));
});
```

- [x] **Step 2: `npx vitest run` 确认失败 → Step 3: 实现 `state.ts`（纯逻辑 + `invoke` 封装分离，便于测试）→ Step 4: 测试全绿 → Step 5: Commit** `feat(ui): 前端状态与分组排序逻辑`

### Task 6: 三主题 + 基础布局

**Files:**

- Create: `index.html`（替换模板页：头部/chips/列表/添加栏/页脚/toast，结构同 Spec §6）
- Create: `src/themes.css`（`:root[data-theme=…]` 变量：`--bg --bg-blur --border --text --muted --accent --chip-bg --chip-on --shadow`）
- Create: `src/style.css`（仅用变量的结构样式，移植 `mockups/style-samples.html`）
- Create: `src/themes.ts`：`applyTheme(t)`；`system` 模式用 `matchMedia('(prefers-color-scheme: dark)')` 映射 glass/dark 并监听变化
- Modify: `src/main.ts`（占位数据渲染验证布局）

- [x] **Step 1: 实现三个主题变量 + 布局**（毛玻璃在 WebView 透明窗口上用 `backdrop-filter`；纸感主题同时把窗口背景设为不透明 —— 通过 `data-theme` 联动 `body` 背景色即可）
- [x] **Step 2: `npm run tauri dev` 手动切换 `data-theme` 验证**：三主题观感与样例一致、窗口圆角透明正常
- [x] **Step 3: Vitest 主题映射**：`system` + dark 媒体查询 → `dark`；light → `glass`（mock matchMedia）
- [x] **Step 4: Commit** — `feat(ui): 三主题系统与基础布局`

### Task 7: 条目交互（增/删/完成/撤销/行内编辑）

**Files:**

- Create: `src/render.ts`（`render(state)` 全量重渲染：分组 → 条目 DOM；事件委托绑定）
- Modify: `src/state.ts`、`src/main.ts`

- [x] **Step 1: 实现**——勾选：`toggle_todo` + 300ms 后重渲染（完成项沉底）；删除：toast「已删除 · 撤销」5s（撤销 = 重新 `add_todo` 原字段）；行内编辑：`contenteditable=false` 的 span 换 `input`，Enter/Esc 处理；添加栏：Enter 调 `add_todo`
- [x] **Step 2: 手动验收**：样例 HTML 中的每个交互在真实窗口中等价可用；数据重启后仍在
- [x] **Step 3: `npx vitest run` 全绿（state 层新增用例：toggle/delete 回滚）**
- [x] **Step 4: Commit** — `feat(ui): 条目增删改完成与撤销`

### Task 8: 分类与拖拽排序

**Files:**

- Create: `src/drag.ts`
- Modify: `src/render.ts`（chips 渲染、＋ 分类小弹层：名称 + 8 色色板）

- [x] **Step 1: 分类 chips**：点击过滤；`＋` 弹出内联小面板（输入名 + 色板单选）；chip 右键菜单：重命名/换色/删除（确认后其待办移入收件箱）
- [x] **Step 2: 拖拽**：HTML5 DnD，同组内拖动 → 松手提交 `reorder(ids)`；`sortMode !== 'manual'` 时禁用 `draggable`
- [x] **Step 3: 手动验收**：拖拽后重启顺序不变；删除分类后其待办仍在
- [x] **Step 4: Commit** — `feat(ui): 分类管理与拖拽排序`

### Task 9: 窗口生命周期（托盘/自启/开机判定/收起）

**Files:**

- Modify: `src-tauri/src/main.rs`（tray + autostart + 启动判定 + 收起销毁/唤起重建）
- Modify: `src/settings.ts`（新建：⚙ 面板 —— 主题/宽度/自启开关/失焦自动收起）

- [x] **Step 1: 托盘**：图标 + 菜单（显示/隐藏、快速添加、设置、开机自启 ✓、退出）；托盘点击 = 显示窗口；角标文字 = 今日剩余数
- [x] **Step 2: 启动判定**：`setup` 中 `has_today()` → true 则 `show()`，false 仅托盘；窗口位置：`posX/Y` 为空时计算右上角（主屏宽 - 340 - 16, 16）
- [x] **Step 3: 收起**：Esc/头部/页脚「收起」→ invoke `collapse` → Rust `window.destroy()`（状态在 `Mutex<State>` 无损）；唤起 → 重建窗口 → 前端 `get_state()` 重渲染。同时在 `on_window_event` 监听异常关闭并自动重建窗口（Spec §9 WebView 崩溃恢复）
- [x] **Step 4: 自启**：`autostart` 插件 `enable/disable`，随设置开关与 `settings.auto_start` 同步
- [x] **Step 5: 手动验收清单**：开机自启静默启动（改系统时间不便时，直接重启验证）；无今日待办 → 只托盘；收起后任务管理器确认 < 20MB（写回 `docs/memory-baseline.md`）
- [x] **Step 6: `cargo test` + `npx vitest run` 全绿。**
- [x] **Step 7: Commit** — `feat: 窗口生命周期与托盘常驻`

### Task 10: 排序菜单 + 收尾

**Files:**

- Modify: `src/main.ts`（头部 ⛶ 菜单：手动/按到期/按分类/按创建 → `set_settings`）
- Create: `docs/manual-checklist.md`（Spec §11 验收清单：三主题、交互、重启持久化、自启、内存双指标）

- [x] **Step 1: 实现排序菜单与设置面板联动**
- [x] **Step 2: release 构建复核内存**：`npm run tauri build`，安装运行，记录展开/收起数值入 `docs/memory-baseline.md`
- [x] **Step 3: 执行 `docs/manual-checklist.md` 全项**
- [x] **Step 4: Commit** — `feat: 排序切换与 MVP 收尾`
